text=input("Введите текст")
def abbreviation(text):
     words=text.split()
     abbreviation=' '
     for word in words :
         if len(word)>=3:
            abbreviation+=word[0].upper()
     return abbreviation
print("Аббревиатура",abbreviation(text))
